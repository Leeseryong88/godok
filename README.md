# 고덕검색

검색어를 입력하고 Enter를 누르면 중국어 키워드로 변환한 뒤 高德地图(고덕지도) 앱/웹을 엽니다.  
도시는 고덕지도 앱의 현재 위치를 따릅니다.

## 시작하기

```bash
npm install
cp .env.example .env.local
# GEMINI_API_KEY 입력
npm run dev
```

## 환경 변수

```env
GEMINI_API_KEY=
```
